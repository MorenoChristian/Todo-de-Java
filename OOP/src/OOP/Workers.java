package OOP;

public interface Workers {
	
	// all methods are public and abstract even if you don´t write it
	double set_bonus(double gratification);
	
	//every variable in a interface is public, static and final even if u don´t write it
	double base_bonus = 1500;
	
}
